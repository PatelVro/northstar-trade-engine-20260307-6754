version: '3.8'

services:
  # Backend service (API and core logic)
  AegisTrade:
    build:
      context: .
      dockerfile: ./docker/Dockerfile.backend
    container_name: AegisTrade-trading
    restart: unless-stopped
    ports:
      - "${AegisTrade_BACKEND_PORT:-8080}:8080"
    volumes:
      - ./config.json:/app/config.json:ro
      - ./decision_logs:/app/decision_logs
      - /etc/localtime:/etc/localtime:ro  # Sync host time
    environment:
      - TZ=${AegisTrade_TIMEZONE:-Asia/Shanghai}  # Set timezone
    networks:
      - AegisTrade-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # Frontend service (static serving and proxy)
  AegisTrade-frontend:
    build:
      context: .
      dockerfile: ./docker/Dockerfile.frontend
    container_name: AegisTrade-frontend
    restart: unless-stopped
    ports:
      - "${AegisTrade_FRONTEND_PORT:-3000}:80"
    networks:
      - AegisTrade-network
    depends_on:
      - AegisTrade
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s

networks:
  AegisTrade-network:
    driver: bridge